import React from 'react';
import StoryCSS from './s.module.scss';
// import PromiseHero from "./promise_hero.svg"

const Promise = () => {
  return (
    <section className={StoryCSS.promise_cont}>
      <div className={StoryCSS.promise}>
      </div>
    </section>
  );
};

export default Promise;
